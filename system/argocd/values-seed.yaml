argo-cd:
  server:
    metrics: &metrics
      enabled: false
      serviceMonitor:
        enabled: false
  controller:
    metrics: *metrics
  repoServer:
    metrics: *metrics
  redis:
    metrics: *metrics
argocd-apps:
  applicationsets:
    root:
      generators:
        - git:
            repoURL: &repoURL https://github.com/chpiano2000/Homelabing
            revision: &revision master
            directories:
              - path: system/*
              - path: platform/*
      template:
        spec:
          source:
            repoURL: *repoURL
            targetRevision: *revision
